spring:
  application:
    name: rakus-blog-backend
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/rakus-blog
    username: postgres
    password: postgres
